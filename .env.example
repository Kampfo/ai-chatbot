# === REQUIRED ===
# OpenAI API Key (PFLICHT für Chat-Funktionalität)
OPENAI_API_KEY=sk-...

# === OPTIONAL ===
# Environment (production, development, local)
ENVIRONMENT=production

# OpenAI Models
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-large

# Database (SQLite by default - wird in Volume gespeichert)
DATABASE_URL=sqlite:///./data/chatbot.db

# Pinecone (Optional - für erweiterte RAG-Funktionalität)
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=
PINECONE_INDEX_NAME=audit-doc-index

# Frontend URL (wird in Production automatisch gesetzt)
FRONTEND_URL=http://localhost:8000

# Upload Directory (wird in Volume gespeichert)
UPLOAD_DIR=/app/data/uploads
